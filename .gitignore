# 編譯產物（僅排除 repo 根目錄下的輸出檔，不影響 cmd/ 原始碼目錄）
/elk-diagnostics
/elk-diagnostics-*
/dist/
/.cache/
/.tmp/
/artifacts/

# macOS
.DS_Store

# 實測產出的診斷報告（可能含使用者叢集資訊）
/report*.html
/reports/
/check-report.*

# 舊版 collect.sh 與驗證流程的根目錄輸出（向後相容）
/elk-bundle-*/
/bundle-*/

# 舊位置相容；新位置為 artifacts/reference/，已由 /artifacts/ 排除
/V.2.3_script_output/

# 本機產生的產品介紹投影片
/docs/交付/elk-diagnostics-介紹.pptx

# 本機 ES 測試環境產生的自簽 CA、憑證與私鑰
/dev/phase0/certs/
/dev/phase0/runtime/

# 連線設定（含認證憑證，僅保留 .example 範本）
config.yaml

# 每個叢集不同的預期節點清單（只交付 .example 範本）
/expected-es-nodes.txt
