# 驗證狀態

最後更新：2026-08-20。`implemented` 只代表程式與自動化測試完成；`verified` 代表已在真機故障分支確認結果。

## 已完成

- ES 8.14.3／ES 9.0.0：前次 Live 與採集包真機基準線涵蓋 39 個採集端點、52 項診斷結果。
- 現行版本維持 43 個 Elasticsearch 固定端點，另有 4 個選配 Kibana 端點與 8 個選配 Logstash 端點；ES parser／analyzer、Kibana 健康卡與 Logstash root／health／pipeline 卡已完成 403／缺檔／格式錯誤自動化回歸。ES8＋Kibana＋Logstash 8.14.3 的健康採集路線已補真機證據；Logstash 8.16+ Health Report 與異常分支仍未完成。
- P00～P16：ES8 Route B 採集包路線已完成；P05 為多因子條件式通過，P11 受 Basic License 限制。
- M00～M09：ES8 三節點 Live／採集包已完成；M08 partial Nodes API 正確回 `unknown`。
- ES-GAP-06 高可用結構、ES-GAP-08 index block：已由 ES8 真機驗證為 `verified`。
- HTTPS、自簽 CA、Basic Auth、採集包離線分析與 HTML 報告：已驗證。
- Host／Kibana／Logstash 子採集器：Shell 語法、假 HTTP 服務與假 SSH 傳輸自動化回歸完成，狀態為 `implemented`。
- Logstash analyzer／HTML：root 可用性、8.16+ health report、舊版 health report skip、pipeline stats measurements 與獨立服務區塊已完成自動化 bundle 驗證；8.14.3 root／pipeline 採集與 health report skip 已補真機證據。
- Kibana Task Manager、Alerting health 與 Elasticsearch Watcher：已接入採集／分析／HTML；Kibana 健康路線已有真機證據，異常分支仍待驗證。
- 2026-08-20：ES8 三節點＋Kibana 真機 Route B 採集完成（43/43 ES 端點、4/4 Kibana 端點、HTTPS／Basic Auth／預期節點清單），報告產出於 `reports/elk-bundle-20260820-022020/check.html`；Kibana Alerting 以「未設定 permanent encryption key」回報 warning，屬環境設定結果。
- 2026-08-20：ES8 三節點＋Kibana＋Logstash 8.14.3 真機 Route B 採集完成（43/43 ES、4/4 Kibana、Logstash root／node／plugin／pipeline／hot threads 端點）；Logstash `/_health_report` 回 404 並正確標示 `SKIPPED`。報告產出於 `reports/es8-kibana8-logstash8-20260820-092738/check.html`。

## 尚未宣稱真機完成

- 真實 indexing pressure、thread pool rejection、queue backlog。
- 真實 JVM heap／CPU／circuit breaker 負載。
- 真實 CCR follower、ML job/datafeed、planned shutdown/voting exclusion 異常。
- ES9 多節點、跨主機與長時間穩定性。
- 採集包的 `--redact`：目前可能含 index、node、host 與 mapping 欄位名稱。
- 新增 ILM policy inventory、SLM repository 關聯、Data stream health、fielddata eviction 四項的 ES8／ES9 Live 與 Route B 真機基準線。
- Host／Kibana／Logstash 子採集器的 Linux／HTTPS／認證／403／timeout 異常分支真機證據；目前只完成 Logstash 8.14.3 健康採集與舊版 Health Report `SKIPPED`。
- Logstash 8.16+ Health Report 的 green／yellow／red 真機結果、Logstash 認證拒絕與 timeout 分支仍待驗證。

## 判讀規則

- 單次快照不能取代時間序列；高 RAM、CPU、I/O 或 heap 單點值不得直接當根因。
- partial API response 必須是 `unknown`，不能用已回應節點推論全叢集正常。
- P／M 案例的造壓結果只代表測試配方成立，不代表該故障涵蓋所有生產變體。

詳細歷史證據移至 [`歷史/驗證紀錄.md`](./歷史/驗證紀錄.md)；下一輪工作見 [`覆蓋缺口.md`](./覆蓋缺口.md)。
