# 診斷報告細修清單

> 用途：集中記錄本輪報告與診斷卡的細部調整。先逐條討論並確認，全部列項完成後再統一施工。
>
> 狀態定義：`[ ]` 待施工、`[x]` 已完成、`[-]` 暫不採用。

## 已決議待施工

### Shard 大小規劃

- [x] 將「發現」從診斷卡最上層移入「小 shard 數量」區塊。
- [x] 「小 shard 數量」區塊使用可收合明細，預設收合，避免 20 筆明細佔用主要版面。
- [x] 診斷卡最上層只保留摘要與整卡判定，不重複列出小 shard findings。
- [x] 移除最上層摘要中的「小 shard 聚合警告：N 個 ≤1024 MiB 的 primary shard」提示，避免與「小 shard 數量」區塊重複；詳細數值留在該區塊呈現。
- [x] 展開後仍保留目前的 index、shard、store、docs 等明細資訊。

### Ingest pipeline 失敗

- [x] 將「本次判定」與「本次觀測值」合併為單一表格，欄位包含 Pipeline、已處理、失敗、失敗率及判定；原始 `Measurements` 仍保留供結構化資料使用。

### 請求拒絕 (thread pool)

- [x] 將「本次觀測」與「本次觀測值」合併為單一表格，欄位依卡片需求呈現節點／Pool、Rejected（累積）、Completed（累積）及判定；原始 `Measurements` 仍保留供結構化資料使用。
- [x] 保留累積值說明：`Rejected > 0` 代表曾發生拒絕，不代表目前仍持續，需以前後兩次採集差值確認。

### 長時間執行 task

- [x] 有超過門檻的 task 時，使用單一明細表呈現 Task／節點、Action、執行時間、可取消及判定，不再重複顯示「本次觀測值」表。
- [x] 沒有超過門檻的 task 時，顯示單一摘要表，呈現「超過門檻的 task 數量 = 0」及「通過」。
- [x] 移除或避免顯示沒有實際意義的「最長 Task 執行時間：0s」；不得讓 0s 被誤解為整個叢集所有 task 的實際最大執行時間。
- [x] 原始 `Measurements` 保留供結構化資料使用，並保留 300 秒門檻與 task 排除規則說明。

### Cluster pending tasks

- [x] 有 pending task 時，使用單一明細表呈現 Priority／Source、排隊時間、執行中及判定，不再重複顯示「本次觀測值」表。
- [x] 沒有 pending task 時，顯示單一摘要表，呈現「Pending task 數量 = 0」及「通過」。
- [x] 移除或避免顯示沒有實際意義的「最長排隊時間：0s」；不得讓 0s 被誤解為實際測得的最大排隊時間。
- [x] 保留既有排隊時間的警告／嚴重門檻、逐列判定與原始 `Measurements` 結構化資料。

### TLS 憑證到期

- [x] 將「本次判定」與「本次觀測值」合併為單一憑證明細表，欄位依卡片需求呈現憑證、到期日、剩餘天數、類型及判定；原始 `Measurements` 仍保留供結構化資料使用。
- [x] 到期提前量由 30 天調整為 100 天，以配合平均每季一次的健檢週期；此為工具預警門檻，不宣稱為 Elastic 官方固定門檻。
- [x] 保留 identity certificate 過期為嚴重、trust certificate 過期為警告的判定規則。
- [x] License 到期預警提前量同步採用 100 天；TLS 與 License 設定欄位分開，避免未來政策調整互相綁定。

### Data stream 健康

- [x] 將「本次判定」與「本次觀測值」合併為單一明細表，保留 Data stream、狀態、Backing indices、ILM policy、Managed by 及判定欄位。
- [x] 保留叢集層級摘要列「Data stream 總數｜叢集｜N」，並保留上方 data stream 整體狀態結論；不再另外顯示重複的觀測值表。
- [x] 原始 `Measurements` 仍保留供 JSON／結構化資料使用；若未偵測到 data stream，改顯示明確文字，不顯示空表或零值觀測表。

## 待補充討論

後續由使用者逐項提出；確認版面與判定規則後再加入本文件。
